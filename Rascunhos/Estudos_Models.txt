

def <nome-banco-de-dados>(model.Model):
    ...
    name = model.CharField(max_length=100)
    slug = models.SlugField()
    recipe = model.IntegerField()
    email = ...
    description = models.TextField()
    is_valid = models.BooleanField(default=False)
    cover = models.ImageField(upload_to='recipes/covers/%Y/%m/%d/')
    category = models.ForeignKey(Category, on_delete=models.SET_NULL, null=True)

    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        """No admin, irá deixar visível 
        o nome principal de cada registro."""
        
        return self.name



ACESSANDO DADOS DO MODELS PELO SHELL

    registros = <nome-banco-de-dados>.objects.all()  # Retornar todos os dados
    ordernar = registros.order_by('id')  # Crescente
    ordernar = registros.order_by('-id')  # Decrescente
    ordernar = registros.order_by('created_at')  # Decrescente

    for registro in registros:
        print(registro.title)
        print(registro.description)

    registros = <nome-banco-de-dados>.objects.all().first()  # Retornar o primeiro item
    registros = <nome-banco-de-dados>.objects.all().last()  # Retornar o último item

    # Criar e Salvar no Banco:
    novo_registro = <nome-banco-de-dados>()
    novo_registro.name = 'Vitor'
    novo_registro.save()